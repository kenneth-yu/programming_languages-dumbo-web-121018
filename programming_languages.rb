def reformat_languages(languages)
  # your code here
  new_hash = {}
  languages.each do |type, language|
    if type == :oo 
      language.each do |name, info|
        new_hash[name] = style
        new_hash[name] = 
end
